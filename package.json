{
  "name": "costbook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Recipe costing for small restaurants — the costing engine",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
